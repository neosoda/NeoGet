<Page
    x:Class="optimizerDuck.UI.Pages.Optimizations.OptimizationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:optimizerDuck.Common.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ext="clr-namespace:optimizerDuck.Common.Extensions"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:optimization="clr-namespace:optimizerDuck.Domain.Optimizations.Models"
    xmlns:optimizations="clr-namespace:optimizerDuck.UI.Pages.Optimizations"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="OptimizationPage"
    d:DataContext="{d:DesignInstance optimizations:OptimizationPage}"
    d:DesignHeight="650"
    d:DesignWidth="800"
    ui:Design.Background="Black"
    ui:Design.Foreground="White"
    ScrollViewer.CanContentScroll="False"
    Style="{StaticResource PageRootStyle}"
    mc:Ignorable="d">
    <Page.Resources>
        <converters:WidthToColumnConverter x:Key="WidthToColumnConverter" />
        <converters:NumericComparisonToVisibilityConverter
            x:Key="ZeroToVisibilityConverter"
            ComparisonType="Equal"
            Threshold="0" />
        <converters:NumericComparisonToVisibilityConverter
            x:Key="GreaterThanZeroToVisibilityConverter"
            ComparisonType="GreaterThan"
            Threshold="0" />
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
        <converters:BooleanToVisibilityConverter
            x:Key="InverseBooleanToVisibilityConverter"
            False="Visible"
            True="Collapsed" />
        <converters:ValueConditionToVisibilityConverter
            x:Key="NullToVisibilityConverter"
            ConditionType="IsNull"
            False="Visible"
            True="Collapsed" />
    </Page.Resources>

    <Grid>
        <!--  Main Content  -->
        <Grid RowDefinitions="Auto,*" Visibility="{Binding ViewModel.IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
            <!--  Toolbar  -->
            <Border Grid.Row="0" Style="{StaticResource ToolbarSurfaceStyle}">
                <WrapPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <!--  Search  -->
                    <ui:TextBox
                        PlaceholderText="{ext:Loc Common.Search.Placeholder}"
                        Style="{StaticResource SearchBoxStyle}"
                        Text="{Binding ViewModel.SearchText, UpdateSourceTrigger=PropertyChanged}" />

                    <!--  Filter by Risk  -->
                    <ComboBox
                        SelectedIndex="{Binding ViewModel.SelectedRiskFilterIndex}"
                        Style="{StaticResource ComboBoxStyle}"
                        ToolTip="{ext:Loc Common.Filter}"
                        ToolTipService.BetweenShowDelay="160"
                        ToolTipService.Placement="Top">
                        <ComboBoxItem HorizontalContentAlignment="Left">
                            <Grid HorizontalAlignment="Left" ColumnDefinitions="Auto Auto">
                                <ui:SymbolIcon
                                    Margin="0,0,6,0"
                                    HorizontalAlignment="Left"
                                    Symbol="Filter24" />
                                <TextBlock
                                    Grid.Column="1"
                                    HorizontalAlignment="Left"
                                    Text="{ext:Loc Common.Filter.All}" />
                            </Grid>
                        </ComboBoxItem>
                        <ComboBoxItem HorizontalContentAlignment="Left">
                            <Grid HorizontalAlignment="Left" ColumnDefinitions="Auto Auto">
                                <ui:SymbolIcon
                                    Margin="0,0,6,0"
                                    Foreground="{DynamicResource SystemFillColorSuccessBrush}"
                                    Symbol="ShieldCheckmark24" />
                                <TextBlock Grid.Column="1" Text="{ext:Loc Optimizer.UI.Risk.Safe}" />
                            </Grid>
                        </ComboBoxItem>
                        <ComboBoxItem HorizontalContentAlignment="Left">
                            <Grid HorizontalAlignment="Left" ColumnDefinitions="Auto Auto">
                                <ui:SymbolIcon
                                    Margin="0,0,6,0"
                                    Foreground="{DynamicResource SystemFillColorCautionBrush}"
                                    Symbol="Warning24" />
                                <TextBlock Grid.Column="1" Text="{ext:Loc Optimizer.UI.Risk.Moderate}" />
                            </Grid>
                        </ComboBoxItem>
                        <ComboBoxItem HorizontalContentAlignment="Left">
                            <Grid HorizontalAlignment="Left" ColumnDefinitions="Auto Auto">
                                <ui:SymbolIcon
                                    Margin="0,0,6,0"
                                    Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                                    Symbol="ShieldError24" />
                                <TextBlock Grid.Column="1" Text="{ext:Loc Optimizer.UI.Risk.Risky}" />
                            </Grid>
                        </ComboBoxItem>
                    </ComboBox>

                    <!--  Sort By  -->
                    <ComboBox
                        SelectedIndex="{Binding ViewModel.SelectedSortByIndex}"
                        Style="{StaticResource ComboBoxStyle}"
                        ToolTip="{ext:Loc Common.SortBy}"
                        ToolTipService.BetweenShowDelay="160"
                        ToolTipService.Placement="Top">
                        <ComboBoxItem HorizontalContentAlignment="Left">
                            <Grid HorizontalAlignment="Left" ColumnDefinitions="Auto Auto">
                                <ui:SymbolIcon
                                    Margin="0,0,6,0"
                                    HorizontalAlignment="Left"
                                    Symbol="Filter24" />
                                <TextBlock Grid.Column="1" HorizontalAlignment="Left">
                                    <Run Text="{ext:Loc Common.SortBy.Risk}" />
                                    <Run Text=" &amp; " />
                                    <Run Text="{ext:Loc Common.SortBy.Status}" />
                                </TextBlock>
                            </Grid>
                        </ComboBoxItem>
                        <ComboBoxItem HorizontalContentAlignment="Left">
                            <Grid HorizontalAlignment="Left" ColumnDefinitions="Auto Auto">
                                <ui:SymbolIcon
                                    Margin="0,0,6,0"
                                    HorizontalAlignment="Left"
                                    Symbol="TextSortAscending24" />
                                <TextBlock
                                    Grid.Column="1"
                                    HorizontalAlignment="Left"
                                    Text="{ext:Loc Common.SortBy.Name}" />
                            </Grid>
                        </ComboBoxItem>
                        <ComboBoxItem HorizontalContentAlignment="Left">
                            <Grid HorizontalAlignment="Left" ColumnDefinitions="Auto Auto">
                                <ui:SymbolIcon
                                    Margin="0,0,6,0"
                                    HorizontalAlignment="Left"
                                    Symbol="Shield24" />
                                <TextBlock
                                    Grid.Column="1"
                                    HorizontalAlignment="Left"
                                    Text="{ext:Loc Common.SortBy.Risk}" />
                            </Grid>
                        </ComboBoxItem>
                        <ComboBoxItem HorizontalContentAlignment="Left">
                            <Grid HorizontalAlignment="Left" ColumnDefinitions="Auto Auto">
                                <ui:SymbolIcon
                                    Margin="0,0,6,0"
                                    HorizontalAlignment="Left"
                                    Symbol="ToggleRight24" />
                                <TextBlock
                                    Grid.Column="1"
                                    HorizontalAlignment="Left"
                                    Text="{ext:Loc Common.SortBy.Status}" />
                            </Grid>
                        </ComboBoxItem>
                    </ComboBox>

                    <!--  Hide Applied toggle  -->
                    <ToggleButton
                        Margin="0,0,0,0"
                        VerticalAlignment="Stretch"
                        IsChecked="{Binding ViewModel.HideApplied}"
                        Visibility="{Binding ViewModel.HasAppliedOptimizations, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Margin="0,0,6,0" Symbol="EyeOff24" />
                            <TextBlock Text="{ext:Loc Optimizer.Menu.HideApplied}" />
                        </StackPanel>
                    </ToggleButton>
                </WrapPanel>
            </Border>

            <!--  No search results  -->
            <StackPanel
                Grid.Row="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Visibility="{Binding ViewModel.Optimizations.Count, Converter={StaticResource ZeroToVisibilityConverter}}">
                <ui:SymbolIcon
                    Margin="0,0,0,10"
                    HorizontalAlignment="Center"
                    FontSize="36"
                    Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                    Symbol="SearchInfo24" />
                <ui:TextBlock
                    HorizontalAlignment="Center"
                    FontTypography="Body"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    Text="{ext:Loc Bloatware.Search.NoResults}" />
            </StackPanel>

            <!--  Optimization Cards  -->
            <ui:VirtualizingItemsControl
                Grid.Row="1"
                ItemsSource="{Binding ViewModel.Optimizations}"
                Visibility="{Binding ViewModel.Optimizations.Count, Converter={StaticResource GreaterThanZeroToVisibilityConverter}}">
                <ui:VirtualizingItemsControl.Template>
                    <ControlTemplate TargetType="ItemsControl">
                        <ScrollViewer HorizontalScrollBarVisibility="Disabled">
                            <ItemsPresenter />
                        </ScrollViewer>
                    </ControlTemplate>
                </ui:VirtualizingItemsControl.Template>
                <ui:VirtualizingItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid VerticalAlignment="Top" Columns="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ScrollViewer}, Converter={StaticResource WidthToColumnConverter}}" />
                    </ItemsPanelTemplate>
                </ui:VirtualizingItemsControl.ItemsPanel>
                <ui:VirtualizingItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Margin" Value="0,0,12,12" />
                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                    </Style>
                </ui:VirtualizingItemsControl.ItemContainerStyle>

                <ui:VirtualizingItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type optimization:BaseOptimization}">
                        <Border
                            Padding="20,14"
                            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource CornerRadiusMedium}">
                            <Grid RowDefinitions="Auto,Auto,Auto,Auto,*">
                                <ui:TextBlock
                                    Grid.Row="0"
                                    FontTypography="Caption"
                                    Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                    Text="{Binding Id}"
                                    TextWrapping="Wrap" />
                                <ui:TextBlock
                                    Grid.Row="1"
                                    FontTypography="Subtitle"
                                    Text="{Binding Name}"
                                    TextWrapping="Wrap" />

                                <ui:VirtualizingItemsControl
                                    Grid.Row="2"
                                    Margin="0,2,0,2"
                                    ItemsSource="{Binding TagDisplays}">
                                    <ui:VirtualizingItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel HorizontalAlignment="Left" VerticalAlignment="Top" />
                                        </ItemsPanelTemplate>
                                    </ui:VirtualizingItemsControl.ItemsPanel>
                                    <ui:VirtualizingItemsControl.ItemContainerStyle>
                                        <Style TargetType="ContentPresenter">
                                            <Setter Property="Margin" Value="0,0,2,2" />
                                            <Setter Property="HorizontalAlignment" Value="Left" />
                                        </Style>
                                    </ui:VirtualizingItemsControl.ItemContainerStyle>
                                    <ui:VirtualizingItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border
                                                Padding="6,2"
                                                HorizontalAlignment="Left"
                                                Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                                                CornerRadius="{StaticResource CornerRadiusSmall}">
                                                <StackPanel Orientation="Horizontal">
                                                    <ui:SymbolIcon
                                                        Margin="0,0,6,0"
                                                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                        Symbol="{Binding Icon}" />

                                                    <ui:TextBlock
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"
                                                        FontTypography="Caption"
                                                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                        Text="{Binding Display}"
                                                        TextWrapping="Wrap" />
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ui:VirtualizingItemsControl.ItemTemplate>
                                </ui:VirtualizingItemsControl>

                                <ui:TextBlock
                                    Grid.Row="3"
                                    Margin="0,10,0,0"
                                    FontTypography="Body"
                                    Text="{Binding ShortDescription}"
                                    TextTrimming="WordEllipsis"
                                    TextWrapping="Wrap" />

                                <Grid
                                    Grid.Row="4"
                                    Margin="0,10,0,0"
                                    VerticalAlignment="Bottom"
                                    ColumnDefinitions="*,Auto">
                                    <Grid RowDefinitions="Auto,Auto">
                                        <Border
                                            Padding="6,2"
                                            HorizontalAlignment="Left"
                                            CornerRadius="{StaticResource CornerRadiusSmall}">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="{DynamicResource SystemFillColorSuccessBrush}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Risk}" Value="Moderate">
                                                            <Setter Property="Background" Value="{DynamicResource SystemFillColorCautionBrush}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Risk}" Value="Risky">
                                                            <Setter Property="Background" Value="{DynamicResource SystemFillColorCriticalBrush}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <StackPanel Orientation="Horizontal">
                                                <ui:SymbolIcon
                                                    Margin="0,0,6,0"
                                                    Foreground="{DynamicResource TextFillColorInverseBrush}"
                                                    Symbol="{Binding RiskVisual.Icon}" />

                                                <ui:TextBlock
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    FontTypography="Caption"
                                                    Foreground="{DynamicResource TextFillColorInverseBrush}"
                                                    Text="{Binding RiskVisual.Display}"
                                                    TextWrapping="Wrap" />
                                            </StackPanel>
                                        </Border>

                                        <Border
                                            Grid.Row="1"
                                            Margin="0,8,0,0"
                                            Padding="6,2"
                                            HorizontalAlignment="Left"
                                            Background="{DynamicResource CardBackgroundDisabled}"
                                            CornerRadius="{StaticResource CornerRadiusSmall}"
                                            ToolTip="{Binding State.AppliedAt}"
                                            ToolTipService.InitialShowDelay="150"
                                            ToolTipService.Placement="Top"
                                            Visibility="{Binding State.AppliedAt, Converter={StaticResource NullToVisibilityConverter}}">
                                            <StackPanel Orientation="Horizontal">
                                                <ui:SymbolIcon Margin="0,0,6,0" Symbol="CheckmarkCircle24" />
                                                <ui:TextBlock
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    FontTypography="Caption"
                                                    Text="{Binding State.RelativeTime}"
                                                    TextWrapping="Wrap" />
                                            </StackPanel>
                                        </Border>
                                    </Grid>

                                    <StackPanel
                                        Grid.Column="1"
                                        Margin="10,0,0,0"
                                        VerticalAlignment="Bottom"
                                        Orientation="Horizontal">
                                        <ui:ToggleSwitch
                                            HorizontalAlignment="Right"
                                            Command="{Binding DataContext.ViewModel.ToggleOptimizationCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                            CommandParameter="{Binding}"
                                            Cursor="Hand"
                                            IsChecked="{Binding State.IsApplied, Mode=TwoWay}"
                                            IsEnabled="{Binding DataContext.ViewModel.IsProcessing, RelativeSource={RelativeSource AncestorType=Page}, Converter={StaticResource InverseBooleanConverter}}"
                                            Style="{StaticResource ToggleSwitchStyle}" />
                                        <ui:Button
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Bottom"
                                            Background="Transparent"
                                            BorderBrush="Transparent"
                                            Command="{Binding DataContext.ViewModel.ViewSourceOnGitHubCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                            CommandParameter="{Binding}"
                                            Cursor="Hand"
                                            Icon="{ui:SymbolIcon Code24}"
                                            ToolTip="{ext:Loc Optimizer.Menu.ViewSource}" />
                                        <ui:Button
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Bottom"
                                            Appearance="Transparent"
                                            Background="Transparent"
                                            BorderBrush="Transparent"
                                            Command="{Binding DataContext.ViewModel.ShowDetailsCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                            CommandParameter="{Binding}"
                                            Cursor="Hand"
                                            Icon="{ui:SymbolIcon MoreVertical24}" />
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ui:VirtualizingItemsControl.ItemTemplate>
            </ui:VirtualizingItemsControl>
        </Grid>

        <!--  Loading Indicator  -->
        <Border Visibility="{Binding ViewModel.IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ui:ProgressRing
                    Width="48"
                    Height="48"
                    Margin="0,0,0,16"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    IsIndeterminate="True" />
                <ui:TextBlock
                    HorizontalAlignment="Center"
                    FontTypography="BodyStrong"
                    Text="{ext:Loc Optimizations.Loading}" />
                <ui:TextBlock
                    Margin="0,4,0,0"
                    HorizontalAlignment="Center"
                    FontTypography="Caption"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    Text="{ext:Loc Optimizations.Loading.Hint}" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
