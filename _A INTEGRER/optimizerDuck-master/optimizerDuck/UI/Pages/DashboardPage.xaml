<Page
    x:Class="optimizerDuck.UI.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:optimizerDuck.Common.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ext="clr-namespace:optimizerDuck.Common.Extensions"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pages="clr-namespace:optimizerDuck.UI.Pages"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Dashboard"
    d:DataContext="{d:DesignInstance pages:DashboardPage}"
    d:DesignHeight="650"
    d:DesignWidth="800"
    ui:Design.Background="Black"
    ui:Design.Foreground="White"
    ScrollViewer.CanContentScroll="False"
    Style="{StaticResource PageRootStyle}"
    mc:Ignorable="d">
    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:MBToGBConverter x:Key="MBToGBConverter" />
        <converters:NumericComparisonToVisibilityConverter
            x:Key="GreaterThanOneToVisibilityConverter"
            ComparisonType="GreaterThan"
            Threshold="1" />
        <converters:MinusNumberConverter x:Key="MinusOneConverter" NumberToMinus="1" />
        <converters:ThemeToGitHubIconConverter x:Key="ThemeToGitHubIconConverter" />
        <converters:ValueConditionToVisibilityConverter
            x:Key="NullToVisibilityConverter"
            ConditionType="IsNull"
            False="Visible"
            True="Collapsed" />
    </Page.Resources>

    <Grid RowDefinitions="Auto,Auto,*">
        <!--  Loading Overlay  -->
        <Grid
            Grid.RowSpan="3"
            Panel.ZIndex="1000"
            Visibility="{Binding ViewModel.IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <Border Background="{DynamicResource ApplicationBackgroundBrush}" Opacity="0.8" />
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ui:ProgressRing
                    Width="60"
                    Height="60"
                    IsIndeterminate="True" />
                <ui:TextBlock
                    Margin="0,16,0,0"
                    HorizontalAlignment="Center"
                    FontTypography="Subtitle"
                    Text="{ext:Loc Dashboard.SystemInfo.Loading}"
                    TextWrapping="Wrap" />
            </StackPanel>
        </Grid>

        <!--  Header  -->
        <StackPanel Style="{StaticResource PageHeaderStyle}">
            <ui:InfoBar
                Margin="0,0,0,12"
                IsOpen="{Binding ViewModel.IsUpdateInfoOpen, Mode=TwoWay}"
                Message="{ext:Loc Dashboard.UpdateInfoBar.Message,
                                  {Binding ViewModel.LatestVersion}}"
                Severity="Warning" />
            <Grid ColumnDefinitions="*,Auto">
                <ui:TextBlock Style="{StaticResource PageTitleStyle}" Text="{ext:Loc Dashboard.Header.Title}" />

                <WrapPanel
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                    <ui:Button
                        Margin="0,0,8,0"
                        Appearance="Secondary"
                        Command="{Binding ViewModel.RunCommand}"
                        CommandParameter="TaskManager"
                        Content="{ext:Loc Dashboard.Header.Menu.RunTaskManager}"
                        Icon="{ui:SymbolIcon Apps24}" />
                    <ui:Button
                        Appearance="Primary"
                        Command="{Binding ViewModel.RefreshCommand}"
                        Content="{ext:Loc Dashboard.Header.Menu.Refresh}"
                        Icon="{ui:SymbolIcon ArrowClockwise24}" />
                </WrapPanel>
            </Grid>
        </StackPanel>

        <!--  Action Cards  -->
        <UniformGrid
            Grid.Row="1"
            Margin="0,0,0,24"
            Columns="3">
            <ui:CardAction
                Margin="0,0,12,0"
                Padding="16,14"
                VerticalAlignment="Stretch"
                Command="{Binding ViewModel.OpenActionCommand}"
                CommandParameter="Discord"
                Cursor="Hand">
                <ui:CardAction.Icon>
                    <ui:ImageIcon Height="25" Source="pack://application:,,,/Resources/Images/DiscordLogo.png" />
                </ui:CardAction.Icon>
                <StackPanel>
                    <ui:TextBlock FontTypography="BodyStrong" Text="{ext:Loc Dashboard.Actions.Discord.Title}" />
                    <ui:TextBlock
                        FontTypography="Caption"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="{ext:Loc Dashboard.Actions.Discord.Description}"
                        TextWrapping="Wrap" />
                </StackPanel>
            </ui:CardAction>

            <ui:CardAction
                Margin="0,0,12,0"
                Padding="16,14"
                VerticalAlignment="Stretch"
                Command="{Binding ViewModel.OpenActionCommand}"
                CommandParameter="GitHub"
                Cursor="Hand">
                <ui:CardAction.Icon>
                    <ui:ImageIcon Height="40" Source="{Binding ViewModel.CurrentApplicationTheme, Converter={StaticResource ThemeToGitHubIconConverter}}" />
                </ui:CardAction.Icon>
                <StackPanel>
                    <ui:TextBlock FontTypography="BodyStrong" Text="{ext:Loc Dashboard.Actions.GitHub.Title}" />
                    <ui:TextBlock
                        FontTypography="Caption"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="{ext:Loc Dashboard.Actions.GitHub.Description}"
                        TextWrapping="Wrap" />
                </StackPanel>
            </ui:CardAction>

            <ui:CardAction
                Margin="0"
                Padding="16,14"
                VerticalAlignment="Stretch"
                BorderThickness="1"
                Command="{Binding ViewModel.OpenActionCommand}"
                CommandParameter="Contribute"
                Cursor="Hand">
                <ui:CardAction.Icon>
                    <ui:SymbolIcon
                        Filled="True"
                        Foreground="{DynamicResource SystemFillColorCautionBrush}"
                        Symbol="Star24" />
                </ui:CardAction.Icon>

                <StackPanel>
                    <ui:TextBlock FontTypography="BodyStrong" Text="{ext:Loc Dashboard.Actions.SupportMe.Title}" />
                    <ui:TextBlock
                        FontTypography="Caption"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="{ext:Loc Dashboard.Actions.SupportMe.Description}"
                        TextWrapping="Wrap" />
                </StackPanel>
            </ui:CardAction>
        </UniformGrid>

        <ScrollViewer
            Grid.Row="2"
            Padding="0"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,12,0">
                <!--  System Info Cards  -->
                <UniformGrid Columns="2" Rows="2">
                    <!--  Operating System  -->
                    <ui:CardControl
                        Margin="0,0,12,12"
                        VerticalAlignment="Stretch"
                        Icon="{ui:SymbolIcon Tv24}">
                        <ui:CardControl.Header>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0">
                                    <ui:TextBlock FontTypography="BodyStrong" Text="{Binding ViewModel.SystemInfo.Os.Name}" />
                                    <ui:TextBlock FontTypography="Caption" Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                                        <Run Text="{Binding ViewModel.SystemInfo.Os.Edition}" />
                                        <Run Text="·" />
                                        <Run Text="{Binding ViewModel.SystemInfo.Os.Architecture}" />
                                    </ui:TextBlock>
                                </StackPanel>

                                <WrapPanel
                                    Grid.Row="1"
                                    Margin="0,8,0,0"
                                    Orientation="Horizontal">
                                    <Border
                                        Margin="0,0,4,4"
                                        Padding="6,2"
                                        Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                                        CornerRadius="{StaticResource CornerRadiusSmall}">
                                        <ui:TextBlock FontTypography="Caption" Text="{ext:Loc Dashboard.SystemInfo.Os.Build, {Binding ViewModel.SystemInfo.Os.BuildNumber}}" />
                                    </Border>
                                    <Border
                                        Margin="0,0,4,4"
                                        Padding="6,2"
                                        Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                                        CornerRadius="{StaticResource CornerRadiusSmall}">
                                        <ui:TextBlock FontTypography="Caption" Text="{Binding ViewModel.SystemInfo.Os.DeviceType}" />
                                    </Border>
                                    <Border
                                        Margin="0,0,4,4"
                                        Padding="6,2"
                                        Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                                        CornerRadius="{StaticResource CornerRadiusSmall}">
                                        <ui:TextBlock FontTypography="Caption" Text="{Binding ViewModel.SystemInfo.Os.InstallDate}" />
                                    </Border>
                                </WrapPanel>
                            </Grid>
                        </ui:CardControl.Header>
                    </ui:CardControl>

                    <!--  CPU  -->
                    <ui:CardControl
                        Margin="0,0,0,12"
                        VerticalAlignment="Stretch"
                        Icon="{ui:SymbolIcon DeveloperBoard24}">
                        <ui:CardControl.Header>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0">
                                    <ui:TextBlock
                                        FontTypography="BodyStrong"
                                        Text="{Binding ViewModel.SystemInfo.Cpu.Name}"
                                        TextTrimming="CharacterEllipsis" />
                                    <ui:TextBlock FontTypography="Caption" Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                                        <Run Text="{Binding ViewModel.SystemInfo.Cpu.Vendor}" />
                                        <Run Text="·" />
                                        <Run Text="{ext:Loc Dashboard.SystemInfo.Cpu.Cores, {Binding ViewModel.SystemInfo.Cpu.Cores}}" />
                                        <Run Text="/" />
                                        <Run Text="{ext:Loc Dashboard.SystemInfo.Cpu.Threads, {Binding ViewModel.SystemInfo.Cpu.Threads}}" />
                                    </ui:TextBlock>
                                </StackPanel>

                                <WrapPanel
                                    Grid.Row="1"
                                    Margin="0,8,0,0"
                                    Orientation="Horizontal">
                                    <Border
                                        Margin="0,0,4,4"
                                        Padding="6,2"
                                        Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                                        CornerRadius="{StaticResource CornerRadiusSmall}">
                                        <ui:TextBlock FontTypography="Caption">
                                            <Run Text="{Binding ViewModel.SystemInfo.Cpu.CurrentClockMHz}" />
                                            <Run Text="MHz" />
                                        </ui:TextBlock>
                                    </Border>
                                    <Border
                                        Margin="0,0,4,4"
                                        Padding="6,2"
                                        Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                                        CornerRadius="{StaticResource CornerRadiusSmall}">
                                        <ui:TextBlock FontTypography="Caption">
                                            <Run Text="L2:" />
                                            <Run Text="{Binding ViewModel.SystemInfo.Cpu.L2CacheKB}" />
                                            <Run Text="KB" />
                                        </ui:TextBlock>
                                    </Border>
                                    <Border
                                        Margin="0,0,4,4"
                                        Padding="6,2"
                                        Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                                        CornerRadius="{StaticResource CornerRadiusSmall}">
                                        <ui:TextBlock FontTypography="Caption">
                                            <Run Text="L3:" />
                                            <Run Text="{Binding ViewModel.SystemInfo.Cpu.L3CacheKB}" />
                                            <Run Text="KB" />
                                        </ui:TextBlock>
                                    </Border>
                                </WrapPanel>
                            </Grid>
                        </ui:CardControl.Header>
                    </ui:CardControl>

                    <!--  RAM  -->
                    <ui:CardControl
                        Margin="0,0,12,0"
                        VerticalAlignment="Stretch"
                        Icon="{ui:SymbolIcon Memory16}">
                        <ui:CardControl.Header>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0">
                                    <ui:TextBlock FontTypography="BodyStrong">
                                        <Run Text="{Binding ViewModel.RuntimeRam.TotalGB, StringFormat='{}{0:F1}'}" />
                                        <Run Text="GB RAM" />
                                    </ui:TextBlock>
                                    <ui:TextBlock FontTypography="Caption" Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                                        <Run Text="{ext:Loc Dashboard.SystemInfo.Ram.Used, {Binding ViewModel.RuntimeRam.UsedGB, StringFormat='{}{0:F1}'}}" />
                                        <Run Text="·" />
                                        <Run Text="{ext:Loc Dashboard.SystemInfo.Ram.Available, {Binding ViewModel.RuntimeRam.AvailableGB, StringFormat='{}{0:F1}'}}" />
                                    </ui:TextBlock>
                                </StackPanel>

                                <ProgressBar
                                    Grid.Row="1"
                                    Height="6"
                                    Margin="0,8,0,0"
                                    Maximum="100"
                                    Value="{Binding ViewModel.RuntimeRam.UsedPercent}" />

                                <WrapPanel
                                    Grid.Row="2"
                                    Margin="0,8,0,0"
                                    Orientation="Horizontal">
                                    <Border
                                        Padding="6,2"
                                        Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                                        CornerRadius="{StaticResource CornerRadiusSmall}">
                                        <ui:TextBlock FontTypography="Caption">
                                            <Run Text="{ext:Loc Dashboard.SystemInfo.Ram.Modules, {Binding ViewModel.RuntimeRam.Modules.Count, Mode=OneWay}}" />
                                        </ui:TextBlock>
                                    </Border>
                                </WrapPanel>
                            </Grid>
                        </ui:CardControl.Header>
                    </ui:CardControl>

                    <!--  GPU  -->
                    <ui:CardControl
                        Margin="0"
                        VerticalAlignment="Stretch"
                        Icon="{ui:SymbolIcon NetworkAdapter16}">
                        <ui:CardControl.Header>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0">
                                    <ui:TextBlock
                                        FontTypography="BodyStrong"
                                        Text="{Binding ViewModel.SystemInfo.PrimaryGpu.Name}"
                                        TextTrimming="CharacterEllipsis" />
                                    <ui:TextBlock
                                        FontTypography="Caption"
                                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                        Text="{Binding ViewModel.SystemInfo.PrimaryGpu.Vendor}" />
                                </StackPanel>

                                <WrapPanel
                                    Grid.Row="1"
                                    Margin="0,8,0,0"
                                    Orientation="Horizontal">
                                    <Border
                                        Margin="0,0,4,4"
                                        Padding="6,2"
                                        Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                                        CornerRadius="{StaticResource CornerRadiusSmall}">
                                        <ui:TextBlock FontTypography="Caption">
                                            <Run Text="{Binding ViewModel.SystemInfo.PrimaryGpu.MemoryMB, Converter={StaticResource MBToGBConverter}}" />
                                            <Run Text="GB" />
                                        </ui:TextBlock>
                                    </Border>
                                    <Border
                                        Margin="0,0,4,4"
                                        Padding="6,2"
                                        Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                                        CornerRadius="{StaticResource CornerRadiusSmall}">
                                        <ui:TextBlock
                                            FontTypography="Caption"
                                            Text="{Binding ViewModel.SystemInfo.PrimaryGpu.DriverVersion}"
                                            TextTrimming="CharacterEllipsis" />
                                    </Border>
                                    <Border
                                        Margin="0,0,4,4"
                                        Padding="6,2"
                                        Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                                        CornerRadius="{StaticResource CornerRadiusSmall}"
                                        Visibility="{Binding ViewModel.SystemInfo.Gpus.Count, Converter={StaticResource GreaterThanOneToVisibilityConverter}}">
                                        <ui:TextBlock FontTypography="Caption">
                                            <Run Text="{ext:Loc Dashboard.SystemInfo.Gpu.More, {Binding ViewModel.SystemInfo.Gpus.Count, Mode=OneWay, Converter={StaticResource MinusOneConverter}}}" />
                                        </ui:TextBlock>
                                    </Border>
                                </WrapPanel>
                            </Grid>
                        </ui:CardControl.Header>
                    </ui:CardControl>
                </UniformGrid>

                <!--  Storage Section  -->
                <ui:TextBlock
                    Margin="0,20,0,8"
                    FontTypography="BodyStrong"
                    Text="{ext:Loc Dashboard.SystemInfo.Storage.Header}" />

                <ItemsControl
                    ItemsSource="{Binding ViewModel.RuntimeDisk.Volumes}"
                    VirtualizingStackPanel.IsVirtualizing="True"
                    VirtualizingStackPanel.VirtualizationMode="Recycling">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ui:CardAction
                                Margin="0,0,0,8"
                                Command="{Binding DataContext.ViewModel.SelectDiskCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}"
                                Cursor="Hand">
                                <ui:CardAction.Icon>
                                    <ui:SymbolIcon Filled="True" Symbol="HardDrive20">
                                        <ui:SymbolIcon.Style>
                                            <Style TargetType="ui:SymbolIcon">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSystemDrive}" Value="True">
                                                        <Setter Property="Foreground" Value="{DynamicResource SystemFillColorAttentionBrush}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ui:SymbolIcon.Style>
                                    </ui:SymbolIcon>
                                </ui:CardAction.Icon>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <!--  Drive Letter and Label  -->
                                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                                        <ui:TextBlock
                                            Margin="0,0,8,0"
                                            FontTypography="BodyStrong"
                                            Text="{Binding DriveLetter, StringFormat='({0})'}" />

                                        <ui:TextBlock
                                            Margin="0,0,8,0"
                                            VerticalAlignment="Center"
                                            FontTypography="Caption"
                                            Text="{Binding VolumeLabel}" />

                                        <Border
                                            Margin="0,0,4,0"
                                            Padding="5,2"
                                            VerticalAlignment="Center"
                                            Background="{DynamicResource SystemFillColorAttentionBrush}"
                                            CornerRadius="{StaticResource CornerRadiusSmall}"
                                            Visibility="{Binding IsSystemDrive, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <ui:TextBlock
                                                FontTypography="Caption"
                                                Foreground="Black"
                                                Text="{ext:Loc Dashboard.SystemInfo.Storage.System}" />
                                        </Border>

                                        <Border
                                            Margin="0,0,4,0"
                                            Padding="5,2"
                                            VerticalAlignment="Center"
                                            Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                                            CornerRadius="{StaticResource CornerRadiusSmall}">
                                            <ui:TextBlock FontTypography="Caption" Text="{Binding MediaType}">
                                                <ui:TextBlock.Style>
                                                    <Style TargetType="ui:TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding MediaType}" Value="SSD">
                                                                <Setter Property="Foreground" Value="{DynamicResource SystemFillColorSuccessBrush}" />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding MediaType}" Value="HDD">
                                                                <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCautionBrush}" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ui:TextBlock.Style>
                                            </ui:TextBlock>
                                        </Border>

                                        <Border
                                            Padding="5,2"
                                            VerticalAlignment="Center"
                                            Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                                            CornerRadius="{StaticResource CornerRadiusSmall}">
                                            <ui:TextBlock FontTypography="Caption" Text="{Binding DriveFormat}" />
                                        </Border>
                                    </StackPanel>

                                    <!--  Model Information  -->
                                    <ui:TextBlock
                                        Grid.Row="1"
                                        Margin="0,4,0,0"
                                        FontTypography="Caption"
                                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                        Text="{Binding Model}"
                                        TextTrimming="CharacterEllipsis"
                                        Visibility="{Binding Model, Converter={StaticResource NullToVisibilityConverter}}" />

                                    <!--  Storage Stats  -->
                                    <StackPanel
                                        Grid.Row="2"
                                        Margin="0,4,0,0"
                                        Orientation="Horizontal">
                                        <ui:TextBlock
                                            FontTypography="Caption"
                                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                            Text="{ext:Loc Dashboard.SystemInfo.Storage.DiskInfo,
                                                           {Binding UsedSizeGB, StringFormat='{}{0:F1}'},
                                                           {Binding TotalSizeGB, StringFormat='{}{0:F1}'},
                                                           {Binding AvailableSizeGB, StringFormat='{}{0:F1}'}}" />
                                    </StackPanel>

                                    <!--  Progress Bar  -->
                                    <ProgressBar
                                        Grid.Row="3"
                                        Height="6"
                                        Margin="0,8,0,0"
                                        Maximum="100"
                                        Value="{Binding UsedPercent}" />
                                </Grid>
                            </ui:CardAction>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
