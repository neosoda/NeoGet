<Page
    x:Class="optimizerDuck.UI.Pages.FeaturesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:optimizerDuck.Common.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ext="clr-namespace:optimizerDuck.Common.Extensions"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pages="clr-namespace:optimizerDuck.UI.Pages"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:optimizerDuck.UI.ViewModels.Pages"
    Title="Features"
    d:DataContext="{d:DesignInstance pages:FeaturesPage}"
    d:DesignHeight="650"
    d:DesignWidth="800"
    ui:Design.Background="Black"
    ui:Design.Foreground="White"
    ScrollViewer.CanContentScroll="False"
    Style="{StaticResource PageRootStyle}"
    mc:Ignorable="d">
    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:WidthToColumnConverter x:Key="WidthToColumnConverter" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Style="{StaticResource PageHeaderStyle}">
            <ui:TextBlock Style="{StaticResource PageTitleStyle}" Text="{ext:Loc Features.Title}" />
            <ui:TextBlock Style="{StaticResource PageDescriptionStyle}" Text="{ext:Loc Features.Description}" />
        </StackPanel>

        <ui:VirtualizingItemsControl
            Grid.Row="1"
            Margin="0"
            ItemsSource="{Binding ViewModel.Categories}">
            <ui:VirtualizingItemsControl.Template>
                <ControlTemplate TargetType="ItemsControl">
                    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                        <ItemsPresenter />
                    </ScrollViewer>
                </ControlTemplate>
            </ui:VirtualizingItemsControl.Template>

            <ui:VirtualizingItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid VerticalAlignment="Top" Columns="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ScrollViewer}, Converter={StaticResource WidthToColumnConverter}}" />
                </ItemsPanelTemplate>
            </ui:VirtualizingItemsControl.ItemsPanel>

            <ui:VirtualizingItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Margin" Value="0,0,12,12" />
                    <Setter Property="HorizontalAlignment" Value="Stretch" />
                </Style>
            </ui:VirtualizingItemsControl.ItemContainerStyle>

            <ui:VirtualizingItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:FeatureCategoryItemViewModel}">
                    <ui:CardAction
                        Padding="20,18"
                        Command="{Binding DataContext.ViewModel.NavigateToCategoryCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                        CommandParameter="{Binding}"
                        IsChevronVisible="False">
                        <Grid RowDefinitions="Auto,Auto,Auto">
                            <ui:SymbolIcon
                                Grid.Row="0"
                                Margin="0,0,0,12"
                                HorizontalAlignment="Left"
                                FontSize="32"
                                Foreground="{DynamicResource AccentYellowBrush}"
                                Symbol="{Binding Icon}" />

                            <ui:TextBlock
                                Grid.Row="1"
                                Margin="0,0,0,6"
                                FontTypography="Subtitle"
                                Text="{Binding Name}"
                                TextWrapping="Wrap" />

                            <ui:TextBlock
                                Grid.Row="2"
                                Appearance="Secondary"
                                FontTypography="Body"
                                Text="{Binding Description}"
                                TextWrapping="Wrap" />
                        </Grid>
                    </ui:CardAction>
                </DataTemplate>
            </ui:VirtualizingItemsControl.ItemTemplate>
        </ui:VirtualizingItemsControl>

        <Border Grid.Row="1" Visibility="{Binding ViewModel.IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ui:ProgressRing
                    Width="48"
                    Height="48"
                    Margin="0,0,0,16"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    IsIndeterminate="True" />
                <ui:TextBlock
                    HorizontalAlignment="Center"
                    FontTypography="BodyStrong"
                    Text="{ext:Loc ToggleFeatures.Loading}" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
