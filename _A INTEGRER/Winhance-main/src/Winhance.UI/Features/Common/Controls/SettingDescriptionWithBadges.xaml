<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Winhance.UI.Features.Common.Controls.SettingDescriptionWithBadges"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctk="using:CommunityToolkit.WinUI.Controls"
    HorizontalAlignment="Stretch"
    HorizontalContentAlignment="Stretch">
    <StackPanel Spacing="6">
        <TextBlock
            Text="{x:Bind DescriptionText, Mode=OneWay}"
            TextWrapping="Wrap"
            Style="{StaticResource CaptionTextBlockStyle}"/>
        <!-- WrapPanel instead of horizontal StackPanel so PowerCfg AC/DC settings
             (which can emit 6–7 pills when HasBattery=true) wrap onto a second row
             on narrow windows instead of getting clipped, while ordinary 3–4-pill
             settings stay on a single row whenever they fit. WrapPanel derives from
             Microsoft.UI.Xaml.Controls.Panel so it works as an ItemsControl.ItemsPanel;
             FlowLayout/WrapLayout from the same packages are VirtualizingLayout-only
             (ItemsRepeater) and not usable here. -->
        <ItemsControl
            ItemsSource="{x:Bind BadgeRow, Mode=OneWay}"
            Visibility="{x:Bind ShowBadges, Mode=OneWay,
                         Converter={StaticResource BooleanToVisibilityConverter}}"
            ItemTemplateSelector="{StaticResource BadgePillTemplateSelector}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <ctk:WrapPanel Orientation="Horizontal"
                                   HorizontalSpacing="6"
                                   VerticalSpacing="4"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </StackPanel>
</UserControl>
